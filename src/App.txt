import { render, screen } from '@testing-library/react'

import App from './App'

// test('renders learn react link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });
// Mock the context module

// jest.mock('react', () => ({
//   ...jest.requireActual('react'),
//   createContext: jest.fn(),
// }));

describe('App component', () => {
  // beforeEach(() => {
  //   // Reset the context mock before each test
  //   jest.clearAllMocks();
  // });
  // afterEach(cleanup)

  it('renders the app correctly', () => {
    render(<App />)
    // const takeControlText = screen.getByText('Take control of your money')
    // const yourIncomeText = screen.getByText('Your Income')
    // const incomeForm = screen.getByRole('form')

    // expect(takeControlText).toBeInTheDocument()
    // expect(yourIncomeText).toBeInTheDocument()
    // expect(incomeForm).toBeInTheDocument();
  })
})
